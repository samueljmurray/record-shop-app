import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    padding: 20,
    alignItems: "center"
  },
  logo: {
    height: 120,
    width: 120,
    resizeMode: "contain"
  }
});